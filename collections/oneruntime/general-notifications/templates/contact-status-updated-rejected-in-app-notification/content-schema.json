{
  "$schema": "http://json-schema.org/draft-04/schema#",
  "title": "Contact status updated (Rejected)",
  "description": "Action event for in-app notification when contact status is updated (rejected)",
  "type": "object",
  "properties": {
    "title": {
      "title": "Title",
      "type": "string",
      "metadata": {
        "placeholderText": "Enter a title (max. 50 chars)",
        "renderingHint": "textarea",
        "customErrorMessages" :  {
          "required": "Please provide a title"
        }
      },
      "default": "Contact status updated (Rejected)"
    },
    "body": {
      "title": "Body",
      "type": "string",
      "metadata": {
        "placeholderText": "Enter a body message (max. 2000 chars)",
        "renderingHint": "textarea",
        "customErrorMessages" :  {
          "required": "Please provide a body text"
        }
      },
      "default": "Contact status updated (Rejected)"
    }
  },
  "required": ["title", "body"]
}
