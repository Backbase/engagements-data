<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="
    http://maven.apache.org/POM/4.0.0
    http://maven.apache.org/xsd/maven-4.0.0.xsd">

    <modelVersion>4.0.0</modelVersion>
    <groupId>com.backbase.modelbank</groupId>
    <artifactId>engagements-data</artifactId>
    <version>0.41.0</version>
    <packaging>pom</packaging>
    <name>Backbase :: Engagement Data</name>

    <properties>
        <backbase-bom.version>2024.04</backbase-bom.version>
        <docker.repo.url>ecr.backbase.com/development</docker.repo.url>
        <gitflow-maven-plugin.version>1.17.0</gitflow-maven-plugin.version>
        <docker-maven-plugin.version>0.40.2</docker-maven-plugin.version>
        <exec.plugin.version>3.0.0</exec.plugin.version>
    </properties>

    <build>
        <pluginManagement>
            <plugins>
                <plugin>
                    <groupId>com.amashchenko.maven.plugin</groupId>
                    <artifactId>gitflow-maven-plugin</artifactId>
                    <version>${gitflow-maven-plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>org.codehaus.mojo</groupId>
                    <artifactId>exec-maven-plugin</artifactId>
                    <version>${exec.plugin.version}</version>
                </plugin>
                <plugin>
                    <groupId>io.fabric8</groupId>
                    <artifactId>docker-maven-plugin</artifactId>
                    <version>${docker-maven-plugin.version}</version>
                    <configuration>
                        <images>
                            <image>
                                <build>
                                    <contextDir>${project.basedir}</contextDir>
                                    <dockerFile>${project.basedir}/ci/docker/Dockerfile</dockerFile>
                                    <imagePullPolicy>IfNotPresent</imagePullPolicy>
                                    <cleanup>remove</cleanup>
                                </build>
                            </image>
                        </images>
                    </configuration>
                </plugin>
            </plugins>
        </pluginManagement>

        <plugins>
            <plugin>
                <groupId>com.amashchenko.maven.plugin</groupId>
                <artifactId>gitflow-maven-plugin</artifactId>
                <configuration>
                    <installProject>false</installProject>
                    <verbose>false</verbose>
                    <gitFlowConfig>
                        <productionBranch>master</productionBranch>
                        <developmentBranch>develop</developmentBranch>
                        <featureBranchPrefix>feature/</featureBranchPrefix>
                        <releaseBranchPrefix>release/</releaseBranchPrefix>
                        <hotfixBranchPrefix>hotfix/</hotfixBranchPrefix>
                        <versionTagPrefix>v</versionTagPrefix>
                        <origin>origin</origin>
                    </gitFlowConfig>
                    <commitMessages>
                        <releaseStartMessage>Update versions for release @{version}</releaseStartMessage>
                        <releaseFinishMessage>Update for next development version @{version}</releaseFinishMessage>
                        <releaseVersionUpdateMessage>Update for next development version @{version}
                        </releaseVersionUpdateMessage>
                    </commitMessages>
                </configuration>
            </plugin>

            <plugin>
                <groupId>org.codehaus.mojo</groupId>
                <artifactId>exec-maven-plugin</artifactId>
                <executions>
                    <execution>
                        <id>create-collections-retail</id>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <phase>compile</phase>
                        <configuration>
                            <executable>${project.basedir}/ci/scripts/create-collections.sh</executable>
                            <arguments>retail</arguments>
                        </configuration>
                    </execution>
                    <execution>
                        <id>create-collections-business</id>
                        <goals>
                            <goal>exec</goal>
                        </goals>
                        <phase>compile</phase>
                        <configuration>
                            <executable>${project.basedir}/ci/scripts/create-collections.sh</executable>
                            <arguments>business</arguments>
                        </configuration>
                    </execution>
                </executions>
            </plugin>
        </plugins>
    </build>

    <profiles>
        <profile>
            <id>docker</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <docker.repo.url>ecr.backbase.com/development</docker.repo.url>
                <docker.skip>false</docker.skip>
                <docker.skipPush>true</docker.skipPush>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>docker-build-retail</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                                <configuration>
                                    <images>
                                        <image>
                                            <name>${docker.repo.url}/${project.artifactId}-retail:${backbase-bom.version}-${project.version}</name>
                                            <build>
                                                <args>
                                                    <PRODUCT_LINE>retail</PRODUCT_LINE>
                                                </args>
                                            </build>
                                        </image>
                                    </images>
                                </configuration>
                            </execution>
                            <execution>
                                <id>docker-build-business</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                                <configuration>
                                    <images>
                                        <image>
                                            <name>${docker.repo.url}/${project.artifactId}-sme:${backbase-bom.version}-${project.version}</name>
                                            <build>
                                                <args>
                                                    <PRODUCT_LINE>business</PRODUCT_LINE>
                                                </args>
                                            </build>
                                        </image>
                                    </images>
                                </configuration>
                            </execution>
                        </executions>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>build-retail</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <docker.skip>false</docker.skip>
                <docker.skipPush>true</docker.skipPush>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>docker-build-retail</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <images>
                                <image>
                                    <name>${docker.repo.url}/${project.artifactId}-retail:${backbase-bom.version}-${project.version}</name>
                                    <build>
                                        <args>
                                            <PRODUCT_LINE>retail</PRODUCT_LINE>
                                        </args>
                                    </build>
                                </image>
                            </images>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>build-business</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <docker.skip>false</docker.skip>
                <docker.skipPush>true</docker.skipPush>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>docker-build-business</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>build</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <images>
                                <image>
                                    <name>${docker.repo.url}/${project.artifactId}-sme:${backbase-bom.version}-${project.version}</name>
                                    <build>
                                        <args>
                                            <PRODUCT_LINE>business</PRODUCT_LINE>
                                        </args>
                                    </build>
                                </image>
                            </images>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>push-retail</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <docker.skipPush>false</docker.skipPush>
                <docker.push.registry>${docker.repo.url}</docker.push.registry>
                <docker.registry>${docker.repo.url}</docker.registry>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>docker-push-retail</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>push</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <registry>${docker.repo.url}</registry>
                            <images>
                                <image>
                                    <name>${docker.repo.url}/${project.artifactId}-retail:${backbase-bom.version}-${project.version}</name>
                                    <build>
                                        <args>
                                            <PRODUCT_LINE>retail</PRODUCT_LINE>
                                        </args>
                                    </build>
                                </image>
                            </images>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>

        <profile>
            <id>push-business</id>
            <activation>
                <activeByDefault>false</activeByDefault>
            </activation>
            <properties>
                <docker.skipPush>false</docker.skipPush>
                <docker.push.registry>${docker.repo.url}</docker.push.registry>
                <docker.registry>${docker.repo.url}</docker.registry>
            </properties>
            <build>
                <plugins>
                    <plugin>
                        <groupId>io.fabric8</groupId>
                        <artifactId>docker-maven-plugin</artifactId>
                        <executions>
                            <execution>
                                <id>docker-push-business</id>
                                <phase>package</phase>
                                <goals>
                                    <goal>push</goal>
                                </goals>
                            </execution>
                        </executions>
                        <configuration>
                            <registry>${docker.repo.url}</registry>
                            <images>
                                <image>
                                    <name>${docker.repo.url}/${project.artifactId}-sme:${backbase-bom.version}-${project.version}</name>
                                    <build>
                                        <args>
                                            <PRODUCT_LINE>business</PRODUCT_LINE>
                                        </args>
                                    </build>
                                </image>
                            </images>
                        </configuration>
                    </plugin>
                </plugins>
            </build>
        </profile>
    </profiles>

</project>
